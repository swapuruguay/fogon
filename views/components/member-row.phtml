<!-- Member Row Component -->
<div class="flex items-center justify-between p-4 bg-white border-b border-gray-100 hover:bg-gray-50 transition" data-collection-type="<?php echo $socio->getCollectionType(); ?>">
  
  <!-- Left: Photo + Name + Document (clickable) -->
  <a href="<?php echo BASE_URL . 'socios/editar/' . $socio->getId(); ?>" class="flex items-center space-x-4 flex-1 min-w-0 hover:opacity-80 transition">
    <div class="flex-shrink-0">
      <img src="<?php echo BASE_URL . 'views/socios/img/' . ($socio->getFoto() ?: 'socio.png'); ?>" 
           class="w-12 h-12 rounded-full object-cover border-2 border-gray-200">
    </div>
    <div class="min-w-0">
      <p class="font-semibold text-gray-900 truncate"><?php echo $socio; ?></p>
      <p class="text-sm text-gray-500">Doc: <?php echo number_format($socio->getDocumento() ?: 0, 0, '', '.'); ?></p>
    </div>
  </a>

  <!-- Center: Collection Type Badge -->
  <div class="px-4">
    <?php if ($socio->isInHouse()): ?>
      <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-semibold bg-emerald-100 text-emerald-800">
        <svg class="w-3 h-3 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
        </svg>
        En el Club
      </span>
    <?php else: ?>
      <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-semibold bg-blue-100 text-blue-800">
        <svg class="w-3 h-3 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
        </svg>
        En Calle
      </span>
    <?php endif; ?>
  </div>

  <!-- Right: Category + Actions -->
  <div class="flex items-center space-x-6">
    
    <!-- Category -->
    <div class="text-right hidden md:block">
      <p class="text-sm font-medium text-gray-800"><?php echo $socio->getCategoria() ? $socio->getCategoria()->getNombre() : 'Sin categoría'; ?></p>
      <p class="text-xs text-gray-500">$ <?php echo number_format($socio->getCategoria() ? $socio->getCategoria()->getImporte() : 0, 2, ',', '.'); ?></p>
    </div>

    <!-- Status Indicator -->
    <div class="text-right hidden lg:block">
      <?php 
      $saldo = $socio->getSaldo() ?? 0;
      if ($saldo <= 0): 
      ?>
        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
          Al día
        </span>
      <?php elseif ($saldo > 0): ?>
        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-red-100 text-red-800">
          Debe $ <?php echo number_format(abs($saldo), 2, ',', '.'); ?>
        </span>
      <?php endif; ?>
    </div>

    <!-- Actions -->
    <div class="flex items-center space-x-2">
      <a href="<?php echo BASE_URL . 'socios/editar/' . $socio->getId(); ?>" 
         class="p-2 text-gray-500 hover:text-blue-600 hover:bg-blue-50 rounded-lg transition" title="Editar">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
        </svg>
      </a>
      <?php if ($socio->isInHouse()): ?>
        <button onclick="registrarPago(<?php echo $socio->getId(); ?>)" 
                class="p-2 text-gray-500 hover:text-emerald-600 hover:bg-emerald-50 rounded-lg transition" title="Registrar Pago">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
        </button>
      <?php else: ?>
        <button onclick="verEstadoRecivo(<?php echo $socio->getId(); ?>)" 
                class="p-2 text-gray-500 hover:text-blue-600 hover:bg-blue-50 rounded-lg transition" title="Ver Estado">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
          </svg>
        </button>
      <?php endif; ?>
    </div>

  </div>
</div>