<div class="max-w-7xl mx-auto px-4">
    <div id="mensaje"></div>
    <form id="form1"  method="POST" class="space-y-4" enctype="multipart/form-data">
      <?php echo csrf_field(); ?>
      <div class="mb-4">
        <label class="font-semibold">Nro de Socio</label>
        <div class="w-full md:w-1/4">
          <input type="text" name='idsoc' id='idsoc' value="<?php echo $this->socio->getId(); ?>" readonly class="border border-gray-300 rounded px-3 py-2 w-full focus:outline-none focus:ring-2 focus:ring-blue-400"> <span><?php echo $this->socio; ?></span>
        </div>
      </div>
      <input type="hidden" name="id" id="id" value=0>
      <div class="mb-4">
        <label class="font-semibold">Nombre</label>
        <div class="w-full md:w-1/3">
          <input type="text" name='nombre' id='nombre' required  class="border border-gray-300 rounded px-3 py-2 w-full focus:outline-none focus:ring-2 focus:ring-blue-400">
        </div>
      </div>
      <div class="mb-4">
        <label class="font-semibold">Apellido</label>
        <div class="w-full md:w-1/3">
          <input type="text" name='apellido' id='apellido' required class="border border-gray-300 rounded px-3 py-2 w-full focus:outline-none focus:ring-2 focus:ring-blue-400">
        </div>

      </div>
      <div class="mb-4">
        <label class="font-semibold">Documento</label>
        <div class="w-full md:w-1/3">
          <label for="check">Deshabilitar para extranjero</label>
          <input type="checkbox" name="check" id="check">
          <input type="text" required onkeypress="return soloNumeros(event);" name='documento' id='documento'  class="border border-gray-300 rounded px-3 py-2 w-full focus:outline-none focus:ring-2 focus:ring-blue-400">
          <span id="alerta" style="color:red; font-weight:bold;"></span>
        </div>

      </div>
      <div class="mb-4">
        <label class="font-semibold">Parentezco</label>
        <div class="w-full md:w-1/3">
          <select name="parentezco" id="parentezco">
            <option value="a">--Parentezco--</option>
            <option value="c">Cónyuge</option>
            <option value="h">Hijo/a</option>
          </select>
        </div>
      </div>
      <div class="mb-4">
        <label class="font-semibold">Sexo</label>
        <div class="w-full md:w-1/3">
          <select name="sexo" class="border border-gray-300 rounded px-3 py-2 w-full focus:outline-none focus:ring-2 focus:ring-blue-400">
            <option value="A">--SEXO--</option>
            <option value="F">Femenino</option>
            <option value="M">Masculino</option>
          </select>
        </div>
      </div>
      <div class="mb-4">
        <label class="font-semibold">Fecha Nacimiento</label>
        <div class="w-full md:w-1/4">
          <input type="date" name='fecha_nacimiento' required id='fecha_nacimiento' class="border border-gray-300 rounded px-3 py-2 w-full focus:outline-none focus:ring-2 focus:ring-blue-400">
        </div>

      </div>
      <input type="submit" value="Guardar" class="bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-4 rounded"/>
  </form>
</div>

<script type="text/javascript">
 const $parents = document.querySelector("parientes")
 let cuerpo = ``
 const frm = document.getElementById('form1')

 frm.addEventListener('submit', async (evt) => {
   evt.preventDefault()
   if(frm.apellido.value == '') {
     alert('Debe ingresar nombre y apellido por serparado')
     return
   }
   let formData = new FormData()
   formData.append('documento', frm.documento.value)
   let res = await fetch('/socios/getParentByDocumento', {
     method: 'post',
     body: formData
   })
   let resultado = await res.json()
   //console.log(resultado)
   if(resultado.documento != 0) {
     let alerta = document.getElementById('alerta')
     alerta.innerHTML = 'Pariente existente en la base de datos, revise'
     frm.documento.focus()
     return
   }
   if(digitoCedula(frm.documento.value)) {
     //frm.submit()
   } else {
     alert('Cédula erronea, verifique')
     frm.documento.focus()
   }
   
   formData.append('nombre', frm.nombre.value)
   formData.append('apellido', frm.apellido.value)
   formData.append('parentezco', frm.parentezco.value)
   formData.append('sexo', frm.sexo.value)
   formData.append('fecha_nacimiento', frm.fecha_nacimiento.value)
   formData.append('idsoc', frm.idsoc.value)
   formData.append('id', frm.id.value)
   let result = await fetch('/socios/guardarParientes', {
     method: 'post',
     body: formData
   })
   let resultado2 = await result.json()
   $div = document.querySelector('#mensaje')
   $div.innerHTML = resultado2.mensaje
   $div.style.color = resultado2.color
   frm.nombre.value = ""
   frm.apellido.value = ""
   frm.parentezco.value = 'a'
   frm.sexo.value = 'A'
   frm.documento.value = ""
   frm.fecha_nacimiento.value = ""
   frm.nombre.focus()
   
 })

function digitoCedula(ci) {
  if(document.getElementById('check').checked) {
    return true
  }
  let digitoIngresado = ci.substring(ci.length -1)
  let cedula = ci.substring(0, ci.length - 1)

  let arrayCedula = cedula.split('')
  if(arrayCedula.length < 6) {
    return false
  } else if (arrayCedula.length == 6) {
    arrayCedula.unshift(0)
  }
  let testigo = [2,9,8,7,6,3,4]
  let sumatoria = 0
  for(let i in testigo) {
    sumatoria+= testigo[i] * arrayCedula[i]
  }

  if((sumatoria % 10) == 0) {
    return 0 == digitoIngresado
  }
  else {
    return 10-(sumatoria % 10) == digitoIngresado
  }


}

function soloNumeros(e)
{

  key = e.keyCode || e.which
  let numeros = "0123456789"
  let teclado = String.fromCharCode(key)
  let especiales = [8,9,37,39,46]
  if(teclado == '.') {
    return false
  }
  let teclado_especial = false
  for(let i in especiales) {

    if(key == especiales[i]) {
      teclado_especial = true
    }
  }

  if(numeros.indexOf(teclado) ==-1 && !teclado_especial) {
    return false
  }
}

</script>
