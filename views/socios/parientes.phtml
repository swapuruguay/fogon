<?php

use App\Core\Session; ?>
<div class="max-w-4xl mx-auto px-4 py-6">

  <!-- Header -->
  <div class="flex items-center justify-between mb-6">
    <div>
      <h1 class="text-2xl font-bold text-gray-900">Parientes</h1>
      <p class="text-gray-500 text-sm mt-1"><?php echo count($this->parientes); ?> familiares registrados</p>
    </div>
    <a href="<?php echo BASE_URL . 'socios/editar/' . $this->idSocio; ?>"
      class="inline-flex items-center px-4 py-2 border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50 transition">
      <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
      </svg>
      Volver
    </a>
  </div>

  <!-- Add Button -->
  <div class="mb-6">
    <a href="<?php echo BASE_URL . 'socios/nuevoPariente/' . $this->idSocio; ?>"
      class="inline-flex items-center px-4 py-2 bg-fogon-900 text-white rounded-lg hover:bg-fogon-800 transition">
      <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
      </svg>
      Agregar Pariente
    </a>
  </div>

  <!-- List -->
  <div class="bg-white rounded-xl shadow-sm border border-gray-100 overflow-hidden">
    <?php if (count($this->parientes) > 0): ?>
      <div class="divide-y divide-gray-100">
        <?php foreach ($this->parientes as $soc): ?>
          <?php
          $parentezco = ($soc->getParentezco() == 'C') ? 'Cónyuge' : 'Hijo/a';
          $sexo = ($soc->getSexo() == 'F') ? 'Femenino' : 'Masculino';
          $badgeClass = ($soc->getParentezco() == 'C') ? 'bg-orange-100 text-orange-700' : 'bg-blue-100 text-blue-700';
          ?>
          <div class="flex items-center justify-between p-4 hover:bg-gray-50 transition">

            <!-- Left: Info -->
            <div class="flex items-center space-x-4 flex-1 min-w-0">
              <div class="w-12 h-12 rounded-full <?php echo $badgeClass; ?> flex items-center justify-center">
                <span class="text-lg font-bold"><?php echo substr($soc->getNombre(), 0, 1) . substr($soc->getApellido(), 0, 1); ?></span>
              </div>
              <div class="min-w-0 flex-1">
                <p class="font-semibold text-gray-900"><?php echo utf8_decode($soc->getNombre()) . ' ' . utf8_decode($soc->getApellido()); ?></p>
                <div class="flex items-center space-x-3 mt-1">
                  <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium <?php echo $badgeClass; ?>">
                    <?php echo $parentezco; ?>
                  </span>
                  <span class="text-xs text-gray-500"><?php echo $sexo; ?></span>
                  <span class="text-xs text-gray-400">•</span>
                  <span class="text-xs text-gray-400">Nac: <?php echo date('d/m/Y', strtotime($soc->getFechaNacimiento())); ?></span>
                </div>
              </div>
            </div>

            <!-- Right: Document + Actions -->
            <div class="flex items-center space-x-4">
              <div class="text-right">
                <p class="text-xs text-gray-400">Documento</p>
                <p class="text-sm font-medium font-mono"><?php echo $soc->getDocumento(); ?></p>
              </div>
              <a href="<?php echo BASE_URL . 'socios/editarPariente/' . $soc->getId(); ?>"
                class="p-2 text-blue-600 hover:bg-blue-50 rounded-lg transition" title="Editar">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
                </svg>
              </a>
              <?php if (!Session::get('usuario')->perfil > 2): ?>
                <a href="<?php echo BASE_URL . 'socios/confirmar/' . $soc->getId(); ?>"
                  class="p-2 text-red-600 hover:bg-red-50 rounded-lg transition" title="Eliminar">
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
                  </svg>
                </a>
              <?php endif; ?>
            </div>
          </div>
        <?php endforeach; ?>
      </div>
    <?php else: ?>
      <div class="p-12 text-center">
        <svg class="mx-auto w-12 h-12 text-gray-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
        </svg>
        <p class="mt-4 text-gray-500">No hay parientes registrados</p>
      </div>
    <?php endif; ?>
  </div>
</div>